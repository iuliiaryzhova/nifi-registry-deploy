apiVersion: v1
kind: Service
metadata:
  name: nifi-registry
spec:
  type: ClusterIP
  selector:
    app: nifi-registry
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
